# 虚拟条码枪助手 (Barcode Generator Tool)

[![.NET 8](https://img.shields.io/badge/.NET-8.0-purple)](https://dotnet.microsoft.com/)
[![WPF](https://img.shields.io/badge/Framework-WPF-blue)](https://github.com/dotnet/wpf)
[![License](https://img.shields.io/badge/License-MIT-green)](LICENSE)

一个专为上位机开发人员设计的辅助工具。在没有实物产品或扫码枪的情况下，通过生成屏幕条码并自动递增，模拟流水线扫码测试场景。

## ✨ 核心功能

* 多码制支持：支持生成 Code 128 (一维码)、QR Code (二维码) 和 Data Matrix (DM码)。
* 智能递增：自动识别字符串末尾的数字并累加（例如：`SN-009` -> `SN-010`），支持补零保持位数。
* 规则管理：可保存常用的条码规则，支持下拉选择、新增、删除。
* 屏幕适配：
    * 窗口置顶 ，方便悬浮在上位机软件之上。
    * 支持拖拽调整窗口大小，自动记忆上次的位置和尺寸。
    * 针对长条码（Code 128）优化了高分辨率渲染，拉宽窗口可提高扫码识别率。
* 系统托盘：关闭窗口时自动最小化到系统托盘，不占用任务栏，双击唤醒。
* 单文件运行：编译为独立的 `.exe` 文件，无需安装，即插即用。

## 🛠️ 技术栈

* 框架：.NET 8.0 (WPF)
* 条码库：ZXing.Net  + ZXing.Net.Bindings.Windows.Compatibility
* 配置存储：System.Text.Json (本地 JSON 文件持久化)

## 🚀 快速开始

### 环境要求
* Windows 10 / 11 (64位)

### 使用说明
1.  输入规则：在输入框填写初始条码（如 `EXEM-5601350S000000000010`）。
2.  生成：条码会自动显示。
3.  下一个：点击“下一个 (+1)”按钮，末尾数字自动 +1 并刷新条码。
4.  保存规则：点击💾按钮保存当前规则到下拉列表。
5.  调整大小：如果条码太密扫不出，请拖动窗口边缘将其拉宽。

## 📦 编译与发布 (Build)

本项目配置为生成单文件自包含 (Self-contained Single File) 程序。

### Visual Studio 发布步骤
1.  右键项目 -> 发布 (Publish)。
2.  选择 文件夹 (Folder)。
3.  配置如下设置：
    * 配置 (Configuration): `Release`
    * 目标框架 (Target Framework): `net8.0-windows`
    * 部署模式 (Deployment Mode): 自由选择
    * 目标运行时 (Target Runtime): `win-x64`
    * 生成单个文件 (Produce single file): `选中`
